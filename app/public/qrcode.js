/* const QRCode = require('qrcode')
const canvas = document.getElementById('canvas')

QRCode.toCanvas(canvas, 'https://github.com/RomainBoudet/Kanban', { errorCorrectionLevel: 'L' }, function (error) {
  if (error) console.error(error)
}) */